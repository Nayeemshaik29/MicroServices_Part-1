spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Nayeemshaik29/Ecom-config-server
          username: ${USERNAME}
            password: ${PASSWORD}
          default-label: master

server:
  port: 8888

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8766/eureka/
